<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
  <entry key="get-current-key">
    SELECT AUTO_INCREMENT
    FROM INFORMATION_SCHEMA.TABLES
    WHERE TABLE_SCHEMA = 'realmaster'
    AND TABLE_NAME = ?;
  </entry>
  <entry key="get-users">
    Select id, username, name, email, mobile_no, status
    FROM mst_user;
  </entry>

  <entry key="get-renter-by-id">
    Select id, lastname, firstname, initial, email,
    mobileno, telno, address1 as address, status
    FROM mst_renter
    WHERE id
    = ?;
  </entry>
  <entry key="get-renter-by-name">
    Select id, lastname, firstname, initial, email,
    mobileno, telno, address1 as address, status
    FROM mst_renter
    WHERE
    lastname = ? AND firstname = ? AND initial = ?
  </entry>

  <entry key="get-users-by-id">
    Select id, username, name, email, mobile_no, status
    FROM mst_user where id = ?;
  </entry>
  <entry key="save-renter">
    INSERT INTO realmaster.mst_renter
    (firstname,
    lastname,
    initial,
    email,
    mobileno,
    telno,
    address1,
    idPresentend,
    emergencyContact,
    status)
    VALUES
    (:firstName,
    :lastName,
    :initial,
    :address,
    :idPresented,
    :mobileNo,
    :telno,
    :email,
    :emergencyContact,
    :status);
  </entry>
   <entry key="update-renter">
    Update realmaster.mst_renter
    SET firstname = :firstName ,
    lastname = :lastName ,
    initial = :initial,
    email = :email,
    mobileno = :mobileNo,
    telno = :telno,
    address1 = :address,
    idPresentend = :idPresented,
    emergencyContact = :emergencyContact,
    status = :status
    WHERE id = :id
  </entry>
  <entry key="save-transaction">
    INSERT INTO `realmaster`.`tx_rental`
      (`id`,
      `aptId`,
      `roomId`,
      `dueDate`,
      `txDate`,
      `startDate`,
      `endDate`,
      `deposit`,
      `renterId`,
      `balance`,
      `txType`,
      `provider`,
      `amount`,
      `status`,
      `userId`,
      `updatedDate`,
      `updtCnt`)
    VALUES
      (:id, 
      :aptId, 
      :roomId, 
      :dueDate, 
      :txDate, 
      :startDate, 
      :endDate, 
      :deposit, 
      :renterId,
      :balance, 
      :txType, 
      :provider, 
      :amount, 
      :status, 
      :userId, 
      :updateDate, 
      :updtCnt);
  </entry>
  <entry key="update-transaction">
    UPDATE `realmaster`.`tx_rental` SET
    `id` = :id,
    `aptId` = :aptId,
    `roomId` = :roomId,
    `dueDate` = :dueDate,
    `txDate` = :txDate,
    `startDate` = :startDate,
    `endDate` = :endDate,
    `deposit` = :deposit,
    `renterId` = :renterId,
    `balance` = :balance,
    `txType` = :txType,
    `provider` = :provider,
    `amount` = :amount,
    `status` = :status,
    `userId` = :userId,
    `updatedDate` = :updateDate,
    `updtCnt` = :updtCnt
    WHERE
    `id`= :id AND `aptId` = :aptId AND `roomId` = :roomId;
  </entry>
  <entry key="get-rental">
   SELECT id, aptId, roomId, dueDate,txDate, startDate, endDate, deposit,
   	renterId, balance, txType, provider, amount, status, userId, updatedDate, updtCnt
   FROM tx_rental WHERE id = ?
  </entry>
  
  <entry key="get-apartments">
    SELECT * FROM realmaster.mst_apartment;
  </entry>
  <entry key="get-apartment">
    SELECT * FROM realmaster.mst_apartment WHERE id = ?;
  </entry>
  <entry key="get-rooms">
    SELECT
    id,
    aptId,
    floor,
    roomNo,
    roomType,
    size,
    occupied,
    telNo,
    rate,
    status
    FROM realmaster.mst_room
    WHERE aptId = ?
  </entry>
  <entry key="get-room-by-id">
    SELECT
    id,
    aptId,
    floor,
    roomNo,
    roomType,
    size,
    occupied,
    telNo,
    rate,
    roomDesc as description,
    status
    FROM realmaster.mst_room
    WHERE id = ?
  </entry>
  <entry key="get-active-renters">
    SELECT
    renter.id,
    renter.firstname,
    renter.lastname,
    renter.initial,
    renter.email,
    renter.mobileno,
    renter.address1,
    renter.telNo,
    renter.address2,
    renter.status
    FROM realmaster.mst_renter as renter
    WHERE status = 'active';
  </entry>
  <entry key="get-renters-info">
    SELECT
    renter.id,
    rent.aptId,
    rent.roomId,
    rent.txType as rentType,
    rent.roomId,
    rent.dueDate,
    rent.startDate,
    rent.endDate,
    rent.deposit,
    rent.balance,
    rent.txType,
    rent.id as txId,
    rent.amount,
    apt.name as aptName,
    room.roomNo as roomName,
    room.rate as roomRate
    FROM realmaster.mst_renter as renter
    LEFT JOIN realmaster.tx_rental as rent
    ON renter.id = rent.renterId
    LEFT JOIN realmaster.mst_apartment as apt
    ON rent.aptId = apt.id
    LEFT JOIN realmaster.mst_room as room
    ON rent.roomId = room.id
    WHERE renter.id = ? and rent.status = 'active';
  </entry>
  <entry key="get-renters-info-by-status">
    SELECT
    renter.id,
    renter.mobileno as mobileNumber,
    rent.aptId,
    rent.roomId,
    rent.txType as rentType,
    rent.roomId,
    rent.dueDate,
    rent.startDate,
    rent.endDate,
    rent.deposit,
    rent.balance,
    rent.txType,
    rent.id as txId,
    rent.amount,
    apt.name as aptName,
    room.roomNo as roomName,
    room.rate as roomRate,
    room.roomDesc as roomDesc
    FROM realmaster.mst_renter as renter
    LEFT JOIN realmaster.tx_rental as rent
    ON renter.id = rent.renterId
    LEFT JOIN realmaster.mst_apartment as apt
    ON rent.aptId = apt.id
    LEFT JOIN realmaster.mst_room as room
    ON rent.roomId = room.id
    WHERE rent.status = ?;
  </entry>
  <entry key="get-transaction-by-renterid">
    SELECT
    id,
    aptId,
    roomId,
    dueDate,
    txDate,
    startDate,
    endDate,
    renterId,
    balance,
    txType,
    status,
    amount
    FROM realmaster.tx_rental
    WHERE renterId = ?
  </entry>
  <entry key="get-transaction-by-renterid-status">
    SELECT
    rental.id,
    rental.aptId,
    apt.name as aptName,
    rental.roomId,
    rental.dueDate,
    rental.txDate,
    rental.startDate,
    rental.endDate,
    rental.renterId,
    rental.balance,
    rental.txType,
    rental.status,
    rental.amount
    FROM realmaster.tx_rental rental
    LEFT JOIN realmaster.mst_apartment apt
    ON rental.aptId = apt.id
    WHERE rental.renterId = ? AND rental.status = ?
  </entry>
    <entry key="get-transaction-by-apt-room">
    SELECT
    rental.id,
    rental.aptId,
    rental.roomId,
    rental.dueDate,
    rental.txDate,
    rental.startDate,
    rental.endDate,
    rental.renterId,
    rental.balance,
    rental.txType,
    rental.status,
    rental.amount
    FROM realmaster.tx_rental rental
	WHERE rental.aptId = ? AND rental.roomId = ? AND rental.status = 'active'
  </entry>
  <entry key="get-transaction-by-apt-room-renter">
    SELECT
    rental.id,
    rental.aptId,
    rental.roomId,
    rental.dueDate,
    rental.txDate,
    rental.startDate,
    rental.endDate,
    rental.renterId,
    rental.balance,
    rental.txType,
    rental.status,
    rental.amount
    FROM realmaster.tx_rental rental
	WHERE rental.aptId = ? AND rental.roomId = ? AND rental.renterId = ? AND rental.status = 'active'
  </entry>
   <entry key="get-renter-by-apt-room">
   SELECT
   a.aptId, 
   a.roomId, 
   a.txType as paymentType, 
   b.id, 
   b.firstname as firstName, 
   b.lastname as lastName, 
   b.initial, 
   b.mobileno as mobileNumber, 
   b.email,
   a.id as txId, 
   a.duedate as dueDate, 
   a.startDate,
   a.duedate as endDate,
   a.deposit, 
   a.balance, 
   a.amount, 
   a.status,
   a.paymentStatus
   FROM tx_rental a left join mst_renter b on a.renterId = b.id
   WHERE a.aptId = ? AND a.roomId = ?
  </entry>
  <entry key="save-collection">
    INSERT INTO realmaster.tx_collections
      (`renterId`,
      `txId`,
      `roomId`,
      `aptId`,
      `currentRoomRate`,
      `amountPaid`,
      `balance`,
      `deposit`,
      `cashChange`,
      `cashReceived`,
      `txDate`,
      `userId`,
      `status`)
    VALUES
      (:renterId, 
      :txId,
      :roomId,
      :aptId,
      :currentRoomRate,
      :amountPaid, 
      :balance, 
      :deposit, 
      :cashChange, 
      :cashReceived, 
      :txDate, 
      :userId, 
      :status);
  </entry>
    <entry key="save-collection-details">
    INSERT INTO realmaster.tx_collections_details
      (`collectionId`,
      `aptId`,
      `roomId`,
      `txId`,
      `amountPaid`,
      `balance`,
      `deposit`,
      `txDate`,
      `status`)
    VALUES
      (:collectionId,
      :aptId,
      :roomId,
      :txId,
      :amountPaid,
      :balance,
      :deposit,
      :txDate,
      :status)
  </entry>
  <entry key="save-room">
    INSERT INTO realmaster.mst_room
   (`id`,
    `aptId`,
    `floor`,
    `roomNo`,
    `roomType`,
    `size`,
    `occupied`,
    `telNo`,
    `rate`,
    `status`)
    VALUES
    (?, ?, ?, ?, ?, ?, ?, ?, ?, ?);
  </entry>
  <entry key="update-room">
    SET @id = ?,
        @aptId = ?,
        @floor = ?,
        @roomNo = ?,
        @roomType = ?,
        @size = ?,
        @occupied = ?,
        @telNo = ?,
        @rate = ?,
        @status = ?;
        
    UPDATE realmaster.mst_room SET
      `id` = @id
      `aptId` = @aptId,
      `floor` = @floor,
      `roomNo` = @roomNo,
      `roomType` = @roomType,
      `size` = @size,
      `occupied` = @occupied,
      `telNo` = @telNo,
      `rate` = @rate,
      `status` = @status
    WHERE id = @id
  </entry>
  <entry key="get-sms-by-id">
    SELECT id, message, sender, recipient, sendDate, receivedDate, 
      messageType, requestId, shortcode, timestamp, status, deleted
    FROM tx_sms WHERE id = ?
  </entry>
    <entry key="get-all-sms">
    SELECT id, message, sender, recipient, sendDate, receivedDate, 
       messageType, requestId, shortcode, timestamp, status, deleted
    FROM tx_sms
  </entry>
  <entry key="update-sms">
    SET @id = ?,
    @message = ?,
    @sender = ?,
    @recipient = ?,
    @sendDate = ?,
    @receivedDate = ?,
    @messageType = ?,
    @requestId = ?,
    @shortcode = ?,
    @timestamp = ?,
    @status = ?,
    @deleted = ?;

    UPDATE realmaster.mst_room SET
    `id` = @id
    `message` = @message,
    `sender` = @sender,
    `recipient` = @recipient,
    `sendDate` = @sendDate,
    `receivedDate` = @receivedDate,
    `messageType` = @messageType,
    `requestId` = @requestId,
    `shortcode` = @shortcode,
    `timestamp` = @timestamp,
    `status` = @status,
    `deleted` = @deleted
    WHERE id = @id
  </entry>
  <entry key="save-sms">
    INSERT INTO realmaster.tx_sms
   (id,
    message,
    sender,
    recipient,
    sendDate,
    receivedDate,
    messageType,
    requestId,
    shortcode,
    timestamp,
    status,
    deleted)
    VALUES
    (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);
  </entry>
  <entry key="electric-findby-apt">
    SELECT el.id, el.aptId, el.roomId, el.currentReading,
	el.previousReading, provider, accountNo,
	diffReading, amount, overdue, el.status, readingDate, dueDate,
	billingNo, generationDate,payment, rm.roomNo as roomNumber, 
    ap.name as apartmentName, meterNo, lastBillNo as billingNo
	FROM mst_electric el LEFT JOIN mst_room rm on
    el.roomId = rm.id
    LEFT JOIN mst_apartment ap on
    el.aptId = ap.id
    WHERE el.aptId = ?
  </entry>
    <entry key="electricprovider-by-provider">
	SELECT * FROM prm_electric WHERE provider = ?
  </entry>
  <entry key="save-billing">
  INSERT INTO `realmaster`.`tx_billing`
  (`id`,
  `aptId`,
  `roomId`,
  `currentReading`,
  `previousReading`,
  `diffReading`,
  `provider`,
  `amount`,
  `overdue`,
  `readingDate`,
  `dueDate`,
  `generationDate`,
  `billingNo`,
  `billType`)
  VALUES
      (:id, 
      :aptId,
      :roomId,
      :currentReading,
      :previousReading,
      :diffReading,
      :provider, 
      :amount, 
      :overdue, 
      :readingDate, 
      :dueDate, 
      :generationDate, 
      :billingNo, 
      :billType);
  </entry>
   <entry key="save-electricbill">
  INSERT INTO `realmaster`.`tx_billing`
  (`id`,
  `aptId`,
  `roomId`,
  `currentReading`,
  `previousReading`,
  `diffReading`,
  `provider`,
  `amount`,
  `overdue`,
  `readingDate`,
  `dueDate`,
  `generationDate`,
  `billingNo`,
  `lastBillNo`,
  `billType`)
  VALUES
      (:id, 
      :aptId,
      :roomId,
      :currentReading,
      :previousReading,
      :diffReading,
      :provider, 
      :amount, 
      :overdue, 
      :readingDate, 
      :dueDate, 
      :generationDate, 
      :billingNo,
      :lastBillNo, 
      :billType);
  </entry>
  <entry key="update-electricbill">
  UPDATE `realmaster`.`mst_electric`
    SET
    `id` = :id,
    `aptId` = :aptId,
    `roomId` = :roomId,
    `accountNo` = :accountNo,
    `currentReading` = :currentReading,
    `previousReading` = :previousReading,
    `provider` = :provider,
    `amount` = :amount,
    `status` = :status,
    `overdue` = :overdue,
    `readingDate` = :readingDate,
    `dueDate` = :dueDate,
    `diffReading` = :diffReading,
    `billingNo` = :billingNo,
    `generationDate` = :generationDate,
    `payment` = :payment,
    `meterNo` = :meterNo,
    `lastBillNo` = :lastBillNo
    WHERE `id` = :id;
  </entry>
  <entry key="billing-findby-generatedDate"">
    SELECT * FROM realmaster.tx_billing;
  </entry>
  <entry key="billing-finby-latest">
    SELECT * from realmaster.tx_billing order by generationDate desc limit 1;
  </entry>
  
</properties>